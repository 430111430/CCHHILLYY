const state = {
    data: {

    }
}

const getters = {
}

// actions
const actions = {

}

// mutations
const mutations = {
    setData(state : any, data : any){
        state.data = data;
    }
}

export default {
    namespaced: true,
    state,
    getters,
    actions,
    mutations
}